1. How react works?
    Create virtual DOMError. when state changes, then compoentn runs 
    diffing(roznicujacy) algorithm that identifies changes
    in this DOMError. Next is update dom with new changes

2. What is React?
    is open-source JS library created by FB for building complex, 
    interactive UI in web and mobile applications.